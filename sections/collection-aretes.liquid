{% comment %}
  Template refactorizado para mostrar productos filtrados por tag
  Con diseño responsive optimizado para móvil y desktop
{% endcomment %}

{% assign target_tag = section.settings.filter_tag | default: "Aretes" %}
{% assign target_tag_lower = target_tag | downcase %}

<!-- Hero Section Responsive -->
<div class="aretes-hero">
    <div class="aretes-hero__container">
        <h1 class="aretes-hero__title">{{ section.settings.title | default: "Aretes" }}</h1>
        {% if section.settings.description != blank %}
            <div class="aretes-hero__description">{{ section.settings.description }}</div>
        {% endif %}
    </div>
</div>

<!-- Products Section -->
<div class="aretes-products">
    <div class="aretes-products__container">
        {% assign all_products = collections.all.products %}

        <!-- Filtros Responsive -->
        <div class="aretes-filters">
            <div class="aretes-filters__buttons">
                <button onclick="filterProducts('all')" id="filter-all" class="filter-btn filter-btn--active">
                    Todos
                </button>
                <button onclick="filterProducts('stud')" id="filter-stud" class="filter-btn">
                    Stud
                </button>
                <button onclick="filterProducts('huggies')" id="filter-huggies" class="filter-btn">
                    Huggies
                </button>
            </div>
            <div class="aretes-filters__count">
                <span id="visible-count"></span> productos encontrados
            </div>
        </div>

        <!-- Grid de productos responsive -->
        {% assign aretes_count = 0 %}
        {% for product in all_products %}
            {% assign product_tags_lower = product.tags | join: ',' | downcase | split: ',' %}
            {% for tag in product_tags_lower %}
                {% assign tag_trimmed = tag | strip %}
                {% if tag_trimmed == target_tag_lower %}
                    {% assign aretes_count = aretes_count | plus: 1 %}
                    {% break %}
                {% endif %}
            {% endfor %}
        {% endfor %}
        
        {% if aretes_count > 0 %}
            <div class="aretes-grid" id="products-grid">
                {% for product in all_products %}
                    {% assign has_tag = false %}
                    {% assign product_tags_lower = product.tags | join: ',' | downcase | split: ',' %}
                    {% for tag in product_tags_lower %}
                        {% assign tag_trimmed = tag | strip %}
                        {% if tag_trimmed == target_tag_lower %}
                            {% assign has_tag = true %}
                            {% break %}
                        {% endif %}
                    {% endfor %}
                    
                    {% if has_tag %}
                        {% assign product_tags = product.tags | join: ',' | downcase %}
                        <div class="product-item" data-tags="{{ product_tags }}">
                            {% render 'product-card', product: product %}
                        </div>
                    {% endif %}
                {% endfor %}
            </div>
        {% else %}
            <!-- Estado vacío -->
            <div class="aretes-empty">
                <svg class="aretes-empty__icon" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M20 13V6a2 2 0 00-2-2H6a2 2 0 00-2 2v7m16 0v5a2 2 0 01-2 2H6a2 2 0 01-2-2v-5m16 0h-2.586a1 1 0 00-.707.293l-2.414 2.414a1 1 0 01-.707.293h-3.172a1 1 0 01-.707-.293l-2.414-2.414A1 1 0 006.586 13H4"></path>
                </svg>
                <h2 class="aretes-empty__title">No hay productos con el tag "{{ target_tag }}"</h2>
                <p class="aretes-empty__text">No se encontraron productos con esta etiqueta.</p>
                <a href="{{ routes.all_products_url }}" class="aretes-empty__button">Ver todos los productos</a>
            </div>
        {% endif %}
    </div>
</div>

<script>
function filterProducts(filterType) {
    const productItems = document.querySelectorAll('.product-item');
    const filterButtons = document.querySelectorAll('.filter-btn');
    const visibleCountElement = document.getElementById('visible-count');
    let visibleCount = 0;

    // Actualizar clases de botones
    filterButtons.forEach(btn => btn.classList.remove('filter-btn--active'));
    document.getElementById('filter-' + filterType).classList.add('filter-btn--active');

    // Filtrar productos
    productItems.forEach(item => {
        const tags = item.dataset.tags;
        const shouldShow = filterType === 'all' || tags.includes(filterType);
        
        item.style.display = shouldShow ? 'block' : 'none';
        if (shouldShow) visibleCount++;
    });

    visibleCountElement.textContent = visibleCount;
}

// Inicializar contador
document.addEventListener('DOMContentLoaded', function() {
    const totalProducts = document.querySelectorAll('.product-item').length;
    document.getElementById('visible-count').textContent = totalProducts;
});
</script>

<style>
/* ========================================
   HERO SECTION RESPONSIVE
   ======================================== */
.aretes-hero {
    background-color: #F5F1E8;
    padding: 2.5rem 1rem;
    text-align: center;
}

.aretes-hero__container {
    max-width: 1280px;
    margin: 0 auto;
}

.aretes-hero__subtitle {
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    color: #8B7355;
    margin-bottom: 1rem;
}

.aretes-hero__title {
    font-family: Georgia, serif;
    font-size: 2rem;
    font-weight: 400;
    color: #2d2d2d;
    margin-bottom: 1rem;
    line-height: 1.2;
}

.aretes-hero__description {
    font-size: 0.95rem;
    color: #8B7355;
    line-height: 1.6;
    max-width: 600px;
    margin: 0 auto;
}

/* ========================================
   PRODUCTS SECTION
   ======================================== */
.aretes-products {
    background-color: white;
    padding: 2rem 0;
    overflow-x: hidden;
    width: 100%;
}

.aretes-products__container {
    max-width: 100%;
    margin: 0 auto;
    padding: 0 1rem;
    overflow-x: hidden;
}

/* ========================================
   FILTROS RESPONSIVE
   ======================================== */
.aretes-filters {
    margin-bottom: 1.5rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.75rem;
}

.aretes-filters__buttons {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.5rem;
    width: 100%;
    max-width: 500px;
}

.filter-btn {
    padding: 0.65rem 1.25rem;
    font-weight: 500;
    font-size: 0.85rem;
    transition: all 0.2s;
    background-color: #e8e8e8;
    color: #666;
    border: none;
    cursor: pointer;
    border-radius: 0;
    flex: 1 1 auto;
    min-width: 90px;
}

.filter-btn--active {
    background-color: #2d2d2d;
    color: white;
}

.filter-btn:hover {
    opacity: 0.9;
}

.aretes-filters__count {
    font-size: 0.8rem;
    color: #999;
}

/* ========================================
   GRID RESPONSIVE - UNA COLUMNA VERTICAL
   ======================================== */
.aretes-grid {
    display: flex !important;
    flex-direction: column !important;
    gap: 1.5rem;
    width: 100%;
    max-width: 100%;
    padding: 0;
}

.product-item {
    transition: opacity 0.3s ease;
    width: 100% !important;
    max-width: 100% !important;
    flex: 0 0 100% !important;
}

.product-item .product-card {
    width: 100% !important;
    max-width: 100% !important;
}

/* ========================================
   ESTADO VACÍO
   ======================================== */
.aretes-empty {
    text-align: center;
    padding: 2rem 1rem;
}

.aretes-empty__icon {
    width: 3.5rem;
    height: 3.5rem;
    margin: 0 auto 1rem;
    color: #ddd;
}

.aretes-empty__title {
    font-size: 1.1rem;
    font-weight: 600;
    color: #2d2d2d;
    margin-bottom: 0.5rem;
}

.aretes-empty__text {
    color: #888;
    margin-bottom: 1.25rem;
    font-size: 0.9rem;
}

.aretes-empty__button {
    display: inline-block;
    padding: 0.65rem 1.25rem;
    background-color: #2d2d2d;
    color: white;
    font-weight: 600;
    text-decoration: none;
    transition: background-color 0.2s;
    font-size: 0.9rem;
}

.aretes-empty__button:hover {
    background-color: #1a1a1a;
}

/* ========================================
   MÓVIL PEQUEÑO (480px+)
   ======================================== */
@media (min-width: 480px) {
    .aretes-products__container {
        padding: 0 1rem;
    }
    
    .aretes-grid {
        gap: 1rem;
    }
    
    .filter-btn {
        flex: 0 1 auto;
        min-width: 100px;
    }
}

/* ========================================
   TABLET PEQUEÑA (640px+)
   ======================================== */
@media (min-width: 640px) {
    .aretes-hero {
        padding: 3rem 1.5rem;
    }
    
    .aretes-hero__title {
        font-size: 2.75rem;
    }
    
    .aretes-hero__description {
        font-size: 1rem;
    }
    
    .aretes-products__container {
        padding: 0 1.5rem;
    }
    
    .aretes-filters {
        gap: 1rem;
    }
    
    .filter-btn {
        padding: 0.7rem 1.5rem;
        font-size: 0.9rem;
    }
}

/* ========================================
   TABLET (768px+)
   ======================================== */
@media (min-width: 768px) {
    .aretes-hero {
        padding: 4rem 2rem;
    }
    
    .aretes-hero__subtitle {
        font-size: 0.75rem;
        margin-bottom: 1.5rem;
    }
    
    .aretes-hero__title {
        font-size: 3.5rem;
        margin-bottom: 1.5rem;
    }
    
    .aretes-hero__description {
        font-size: 1.125rem;
        max-width: 700px;
        line-height: 1.8;
    }
    
    .aretes-products {
        padding: 3rem 0;
    }
    
    .aretes-products__container {
        padding: 0 2rem;
    }
    
    .aretes-filters {
        margin-bottom: 2.5rem;
    }
    
    .aretes-filters__buttons {
        gap: 1rem;
    }
    
    .filter-btn {
        padding: 0.75rem 2rem;
        font-size: 0.9375rem;
    }
    
    .aretes-filters__count {
        font-size: 0.875rem;
    }
    
    .aretes-grid {
        gap: 1.5rem;
    }
    
    .aretes-empty {
        padding: 4rem 2rem;
    }
    
    .aretes-empty__icon {
        width: 5rem;
        height: 5rem;
    }
    
    .aretes-empty__title {
        font-size: 1.5rem;
    }
    
    .aretes-empty__text {
        font-size: 1rem;
    }
    
    .aretes-empty__button {
        padding: 0.75rem 1.5rem;
        font-size: 1rem;
    }
}

/* ========================================
   DESKTOP (1024px+)
   ======================================== */
@media (min-width: 1024px) {
    .aretes-hero {
        padding: 6rem 2rem;
    }
    
    .aretes-hero__title {
        font-size: 5rem;
        margin-bottom: 2rem;
    }
    
    .aretes-hero__description {
        max-width: 800px;
    }
    
    .aretes-hero__subtitle {
        margin-bottom: 2rem;
    }
    
    .aretes-products {
        padding: 4rem 0;
    }
    
    .aretes-filters {
        margin-bottom: 3rem;
        gap: 1.5rem;
    }
    
    .aretes-empty__icon {
        width: 6rem;
        height: 6rem;
        margin-bottom: 1.5rem;
    }
    
    .aretes-empty__title {
        margin-bottom: 0.5rem;
    }
    
    .aretes-empty__text {
        margin-bottom: 2rem;
    }
}

/* ========================================
   DESKTOP GRANDE (1280px+)
   ======================================== */
@media (min-width: 1280px) {
    .aretes-grid {
        gap: 2.5rem;
    }
}
</style>

{% schema %}
{
    "name": "Aretes (Filtrado por Tag)",
    "class": "shopify-section--aretes",
    "settings": [
        {
            "type": "text",
            "id": "filter_tag",
            "label": "Tag para filtrar",
            "default": "aretes",
            "info": "Muestra solo productos que tengan este tag. Asegúrate de etiquetar tus productos con 'aretes'"
        },
        {
            "type": "text",
            "id": "title",
            "label": "Título de la sección",
            "default": "Aretes"
        },
        {
            "type": "textarea",
            "id": "description",
            "label": "Descripción",
            "info": "Descripción opcional para mostrar debajo del título"
        },
        {
            "type": "paragraph",
            "content": "✅ Esta sección muestra TODOS los productos filtrados por el tag 'aretes'. Los botones de filtro (Stud, Huggies) funcionan en el frontend filtrando por subtags adicionales. Etiqueta tus productos con: 'aretes' y opcionalmente 'stud' o 'huggies'."
        }
    ]
}
{% endschema %}
