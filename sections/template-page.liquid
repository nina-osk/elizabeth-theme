<div class="w-full bg-white py-12 md:py-16 lg:py-20 mb-12 md:mb-16">
    <div class="max-w-4xl mx-auto px-6 md:px-12">
        
        <!-- Encabezado de la página -->
        <div class="text-center mb-8 md:mb-12">
            <div class="inline-block mb-4">
                <div class="h-1 w-16 mx-auto mb-4" style="background: linear-gradient(to right, #d4af37, #f4d03f);"></div>
            </div>
            <h1 class="text-3xl md:text-4xl lg:text-5xl font-bold mb-4" style="color: #222121;">
                {{ page.title }}
            </h1>
            <div class="h-1 w-24 mx-auto" style="background: linear-gradient(to right, #d4af37, #f4d03f);"></div>
        </div>

        <!-- Contenido de la página -->
        <div class="prose prose-lg max-w-none">
            <div class="bg-white p-6 md:p-8 lg:p-10 rounded-lg shadow-lg border-t-4" style="border-color: #d4af37;">
                <div class="page-content" style="color: #222121;">
                    {{ page.content }}
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .page-content h1, .page-content h2, .page-content h3 {
        color: #222121;
        font-weight: 700;
        margin-top: 1.5em;
        margin-bottom: 0.75em;
    }
    
    .page-content h2 {
        border-bottom: 2px solid #d4af37;
        padding-bottom: 0.5em;
    }
    
    .page-content a {
        color: #d4af37;
        text-decoration: none;
        font-weight: 600;
        transition: color 0.3s;
    }
    
    .page-content a:hover {
        color: #b8941f;
        text-decoration: underline;
    }
    
    .page-content p {
        line-height: 1.8;
        margin-bottom: 1em;
        color: #222121;
    }
    
    .page-content ul, .page-content ol {
        margin-left: 1.5em;
        margin-bottom: 1em;
        color: #222121;
    }
    
    .page-content li {
        margin-bottom: 0.5em;
        line-height: 1.6;
    }
    
    .page-content li::marker {
        color: #d4af37;
    }
    
    .page-content strong, .page-content b {
        color: #d4af37;
        font-weight: 700;
    }
    
    .page-content blockquote {
        border-left: 4px solid #d4af37;
        padding-left: 1.5em;
        margin: 1.5em 0;
        font-style: italic;
        color: #555;
        background: #f9f9f9;
        padding: 1em 1em 1em 1.5em;
        border-radius: 0 8px 8px 0;
    }
</style>