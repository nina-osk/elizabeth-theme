<script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css" />

<div class="w-full bg-gradient-to-r from-purple-100 to-purple-200">
  <div class="swiper hero-swiper w-full" style="height: 500px;">
    <div class="swiper-wrapper">
      <div class="swiper-slide w-full h-full">
        <div class="relative w-full h-full flex items-center justify-between px-6 md:px-12 lg:px-20 bg-gradient-to-r from-gray-100 to-purple-100">
          <!-- Content Left -->
          <div class="w-full md:w-1/2 pr-6 hero-content">
            <p class="text-amber-600 text-xs md:text-sm font-semibold tracking-widest uppercase mb-3 overflow-hidden">
              <span class="inline-block animate-typing">{{ section.settings.subtitle }}</span>
            </p>
            <h2 class="text-2xl md:text-4xl lg:text-5xl font-bold text-black mb-4 leading-tight animate-fade-in-up" style="animation-delay: 0.4s;">
              {{ section.settings.title }}
            </h2>
            <p class="text-gray-700 text-sm md:text-base leading-relaxed mb-6 animate-fade-in-up" style="animation-delay: 0.6s;">
              {{ section.settings.description }}
            </p>
            <div class="flex gap-3 md:gap-4 animate-fade-in-up" style="animation-delay: 0.8s;">
              <button class="bg-black text-white px-5 md:px-6 py-2 md:py-3 text-xs md:text-sm font-semibold uppercase hover:bg-gray-800 transition-all hover:scale-105 hover:shadow-lg">
                {{ section.settings.button_1_text }}
              </button>
              <button class="border-2 border-black text-black px-5 md:px-6 py-2 md:py-3 text-xs md:text-sm font-semibold uppercase hover:bg-black hover:text-white transition-all hover:scale-105 hover:shadow-lg">
                {{ section.settings.button_2_text }}
              </button>
            </div>
          </div>

          <!-- Circle Right -->
          <div class="hidden md:flex w-1/2 justify-center items-center">
            <div class="relative w-64 h-64 border-2 border-black rounded-full flex items-center justify-center animate-spin-slow">
              <div class="absolute w-56 h-56 border-2 border-dashed border-black rounded-full animate-spin-reverse"></div>
              <div class="text-4xl font-bold text-black animate-pulse-subtle">✦</div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Navigation arrows -->
    <div class="swiper-button-prev absolute left-4 top-1/2 -translate-y-1/2 z-20 w-10 h-10 bg-white/80 hover:bg-white rounded-full flex items-center justify-center cursor-pointer transition-all hover:scale-110 after:!text-black after:!text-lg"></div>
    <div class="swiper-button-next absolute right-4 top-1/2 -translate-y-1/2 z-20 w-10 h-10 bg-white/80 hover:bg-white rounded-full flex items-center justify-center cursor-pointer transition-all hover:scale-110 after:!text-black after:!text-lg"></div>

    <!-- Pagination -->
    <div class="swiper-pagination absolute bottom-4 left-1/2 -translate-x-1/2 z-20"></div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const swiper = new Swiper('.hero-swiper', {
      loop: true,
      autoplay: {
        delay: 5000,
        disableOnInteraction: false,
      },
      navigation: {
        nextEl: '.swiper-button-next',
        prevEl: '.swiper-button-prev',
      },
      pagination: {
        el: '.swiper-pagination',
        type: 'bullets',
        clickable: true,
      },
      speed: 800,
      on: {
        slideChange: function () {
          // Reiniciar animaciones al cambiar de slide
          const activeSlide = this.slides[this.activeIndex];
          const content = activeSlide.querySelector('.hero-content');
          if (content) {
            content.style.animation = 'none';
            setTimeout(() => {
              content.style.animation = '';
            }, 10);
          }
        }
      }
    });
  });
</script>

<style>
  .hero-swiper {
    --swiper-pagination-bullet-size: 12px;
    --swiper-pagination-bullet-horizontal-gap: 10px;
  }
  
  .swiper-button-prev::after,
  .swiper-button-next::after {
    font-size: 18px;
    color: #000;
  }
  
  .swiper-pagination-bullet {
    background-color: rgba(255, 255, 255, 0.6);
    transition: all 0.3s ease;
  }
  
  .swiper-pagination-bullet-active {
    background-color: #fff;
    transform: scale(1.2);
  }

  /* Animaciones */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes typing {
    0% {
      width: 0;
      opacity: 0;
    }
    1% {
      opacity: 1;
    }
    100% {
      width: 100%;
      opacity: 1;
    }
  }

  @keyframes shine {
    0% {
      background-position: -200%;
    }
    100% {
      background-position: 200%;
    }
  }

  @keyframes letterSpacing {
    0%, 100% {
      letter-spacing: 0.1em;
    }
    50% {
      letter-spacing: 0.2em;
    }
  }

  .animate-typing {
    display: inline-block;
    white-space: nowrap;
    overflow: hidden;
    animation: typing 2s steps(40, end) forwards,
               letterSpacing 3s ease-in-out infinite 2s;
    background: linear-gradient(90deg, #d97706, #f59e0b, #fbbf24, #d97706);
    background-size: 200% auto;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: typing 2s steps(40, end) forwards,
               shine 3s linear infinite 2s,
               letterSpacing 3s ease-in-out infinite 2s;
  }

  @keyframes typingTitle {
    0% {
      width: 0;
      opacity: 0;
    }
    1% {
      opacity: 1;
    }
    100% {
      width: 100%;
      opacity: 1;
    }
  }

  @keyframes fadeInScale {
    0% {
      opacity: 0;
      transform: scale(0.95);
    }
    100% {
      opacity: 1;
      transform: scale(1);
    }
  }

  .animate-typing-title {
    display: inline-block;
    overflow: hidden;
    animation: typingTitle 2.5s steps(50, end) 2s forwards,
               fadeInScale 0.5s ease-out 2s forwards;
    opacity: 0;
    background: linear-gradient(135deg, #000000, #1a1a1a, #000000);
    background-size: 200% auto;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  @media (max-width: 768px) {
    .animate-typing,
    .animate-typing-title {
      white-space: normal;
      animation: fadeInUp 1s ease-out forwards;
    }
  }

  @keyframes spinSlow {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  @keyframes spinReverse {
    from {
      transform: rotate(360deg);
    }
    to {
      transform: rotate(0deg);
    }
  }

  @keyframes pulseSubtle {
    0%, 100% {
      transform: scale(1);
      opacity: 1;
    }
    50% {
      transform: scale(1.1);
      opacity: 0.8;
    }
  }

  .animate-fade-in-up {
    animation: fadeInUp 0.8s ease-out forwards;
    opacity: 0;
  }

  .animate-spin-slow {
    animation: spinSlow 20s linear infinite;
  }

  .animate-spin-reverse {
    animation: spinReverse 15s linear infinite;
  }

  .animate-pulse-subtle {
    animation: pulseSubtle 3s ease-in-out infinite;
  }

  /* Efecto hover mejorado para botones */
  button {
    position: relative;
    overflow: hidden;
  }

  button::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 0;
    height: 0;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.1);
    transform: translate(-50%, -50%);
    transition: width 0.6s, height 0.6s;
  }

  button:hover::before {
    width: 300px;
    height: 300px;
  }
</style>

{% schema %}
{
  "name": "Hero Banner",
  "settings": [
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtítulo",
      "default": "JOYERIA ORO 9K ATEMPORAL"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Título",
      "default": "Descubre La Belleza Del Lujo Sutil"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Descripción",
      "default": "En Munier, creemos que el lujo no tiene que ser ostentoso. Es la elegancia silenciosa la que brilla a través de la simplicidad de nuestras piezas."
    },
    {
      "type": "text",
      "id": "button_1_text",
      "label": "Botón 1",
      "default": "MAS DETALLES"
    },
    {
      "type": "text",
      "id": "button_2_text",
      "label": "Botón 2",
      "default": "COMPRAR"
    }
  ],
  "presets": [
    {
      "name": "Hero Banner"
    }
  ]
}
{% endschema %}
