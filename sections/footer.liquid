<footer class="footer-main">
  {% liquid 
    if section.settings.footer_menu.links == blank
      assign footer_menu_links = linklists[section.settings.footer_menu].links
    else
      assign footer_menu_links = section.settings.footer_menu.links
    endif
  %}
  <div class="footer-container">

    <!-- Main Footer Content -->
    <div class="footer-content">
      <div class="footer-grid">
        
        <!-- Brand Column -->
        <div class="footer-column footer-brand-column">
          <a href="{{ routes.root_url }}" class="footer-logo-link">
            {% if section.settings.footer_logo != blank %}
              <img
                src="{{ section.settings.footer_logo | image_url: width: 400 }}"
                alt="{{ shop.name }}"
                width="200"
                height="200"
                loading="lazy"
                class="footer-logo-img"
              >
            {% else %}
              <img
                src="https://cdn.shopify.com/s/files/1/0760/2697/0309/files/Munier_M_verde.jpg?v=1769442279"
                alt="{{ shop.name }}"
                width="200"
                height="200"
                loading="lazy"
                class="footer-logo-img"
              >
            {% endif %}
          </a>
        </div>

        <!-- Navigation Column (from menu) -->
        <div class="footer-column">
          <h3 class="footer-column-title">
            {{ section.settings.explore_title | default: "EXPLORAR" }}
          </h3>
          <nav class="footer-nav">
            {% for link in footer_menu_links %}
              <a href="{{ link.url }}" class="footer-link">
                {{ link.title }}
              </a>
            {% endfor %}
          </nav>
        </div>

        <!-- Munier Column -->
        <div class="footer-column">
          <h3 class="footer-column-title">
            {{ section.settings.menu_title | default: "MUNIER" }}
          </h3>
          <nav class="footer-nav">
            <a href="/pages/contact" class="footer-link">
              Contacto
            </a>
            <a href="/pages/costo-del-envio" class="footer-link">
              Costo de Envío
            </a>
            <a href="/pages/page-politica-devoluciones" class="footer-link">
              Política de Devoluciones y Cambios
            </a>
            <a href="https://wa.me/50768697555" target="_blank" class="footer-link">
              WhatsApp Business
            </a>
          </nav>
        </div>

      </div>
    </div>
    
    <!-- Bottom Bar -->
    <div class="footer-bottom">
      <p class="footer-info">
        {{ section.settings.info_text | default: "Joyería Panameña • Oro 9K & 18K • Diamantes de Laboratorio" }}
      </p>
      <p class="footer-legal">
        {{ section.settings.legal_text_1 | default: "Maison Myck, S. A." }}
      </p>
      <p class="footer-legal">
        {{ section.settings.legal_text_2 | default: "Munier Jewelry" }}
      </p>
    </div>

  </div>
</footer>

<style>
/* ========================================
   FOOTER BASE STYLES
   ======================================== */
.footer-main {
  background-color: #AFC4AE;
  color: #2d2d2d;
  overflow-x: hidden;
}

.footer-container {
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 1rem;
  width: 100%;
  box-sizing: border-box;
}

/* ========================================
   MAIN CONTENT
   ======================================== */
.footer-content {
  padding-top: 2rem;
  padding-bottom: 2rem;
}

.footer-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 2rem;
}

.footer-column {
  text-align: center;
}

.footer-column-title {
  font-size: 0.7rem;
  font-weight: 600;
  color: #5a7058;
  margin-bottom: 1rem;
  letter-spacing: 0.1em;
}

.footer-brand-column {
  display: flex;
  align-items: center;
  justify-content: center;
}

.footer-logo-link {
  display: inline-block;
  text-decoration: none;
}

.footer-logo-img {
  height: auto;
  width: auto;
  max-height: 8rem;
  max-width: 8rem;
  border-radius: 50%;
  object-fit: cover;
}

.footer-nav {
  display: flex;
  flex-direction: column;
  gap: 0.65rem;
}

.footer-link {
  font-size: 0.9rem;
  color: #3d523c;
  text-decoration: none;
  transition: color 0.2s;
}

.footer-link:hover {
  color: #2d2d2d;
}

/* ========================================
   BOTTOM BAR
   ======================================== */
.footer-bottom {
  border-top: 1px solid rgba(93, 112, 88, 0.3);
  padding-top: 1.5rem;
  padding-bottom: 1.5rem;
  text-align: center;
}

.footer-copyright {
  font-size: 0.8rem;
  color: #5a7058;
  margin-bottom: 0.5rem;
}

.footer-info {
  font-size: 0.75rem;
  color: #6b8068;
  margin-bottom: 0.25rem;
}

.footer-legal {
  font-size: 0.7rem;
  color: #6b8068;
  margin-bottom: 0.25rem;
}

/* ========================================
   TABLET (640px+)
   ======================================== */
@media (min-width: 640px) {
  .footer-container {
    padding: 0 1.5rem;
  }
  
  .footer-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 2.5rem;
  }
  
  .footer-column:first-child {
    grid-column: 1 / -1;
  }
}

/* ========================================
   DESKTOP (768px+)
   ======================================== */
@media (min-width: 768px) {
  .footer-container {
    padding: 0 2rem;
  }
  
  .footer-content {
    padding-top: 3rem;
    padding-bottom: 3rem;
  }
  
  .footer-grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 4rem;
  }
  
  .footer-column {
    text-align: left;
  }
  
  .footer-column:first-child {
    grid-column: auto;
  }
  
  .footer-column-title {
    font-size: 0.75rem;
    margin-bottom: 1.5rem;
  }
  
  .footer-brand-column {
    justify-content: flex-start;
  }
  
  .footer-logo-img {
    max-height: 10rem;
    max-width: 10rem;
  }
  
  .footer-nav {
    gap: 0.75rem;
  }
  
  .footer-link {
    font-size: 0.9375rem;
  }
  
  .footer-bottom {
    padding-top: 2rem;
    padding-bottom: 2rem;
  }
  
  .footer-copyright {
    font-size: 0.875rem;
  }
  
  .footer-info {
    font-size: 0.8125rem;
  }

  .footer-legal {
    font-size: 0.75rem;
  }
}
</style>

{% schema %}
{
  "name": "Footer",
  "settings": [
    {
      "type": "header",
      "content": "Logo del Footer"
    },
    {
      "type": "image_picker",
      "id": "footer_logo",
      "label": "Logo del Footer"
    },
    {
      "type": "header",
      "content": "Columna Explorar (Centro)"
    },
    {
      "type": "link_list",
      "id": "footer_menu",
      "label": "Menú de navegación"
    },
    {
      "type": "text",
      "id": "explore_title",
      "label": "Título de Explorar",
      "default": "EXPLORAR"
    },
    {
      "type": "header",
      "content": "Columna del Menú (Derecha)"
    },
    {
      "type": "text",
      "id": "menu_title",
      "label": "Título del menú",
      "default": "MUNIER"
    },
    {
      "type": "header",
      "content": "Pie del Footer"
    },
    {
      "type": "text",
      "id": "info_text",
      "label": "Texto de Información",
      "default": "Joyería Panameña • Oro 9K & 18K • Diamantes de Laboratorio"
    },
    {
      "type": "text",
      "id": "legal_text_1",
      "label": "Texto Legal 1",
      "default": "Maison Myck, S. A."
    },
    {
      "type": "text",
      "id": "legal_text_2",
      "label": "Texto Legal 2",
      "default": "Munier Jewelry"
    }
  ]
}
{% endschema %}
